extends layout.pug

block content

  nav.nav
    a(href='/')
      span &larr;
      |  Back

  article.portfolio-projects
    .inner-wrapper.flex-row-wrap.two-col
      .project-info-box.box
      
        //- dynamically add the project title
        h1= project.project_name

        //- dynamically add the project description
        p.projectInfo= project.description

      .project-tech-links-box.box
        h6 Technologies
        ul

          each technology in project.technologies
            li= technology

        a.btn-link(href=`${project.livelink}` target="_blank") Live Demo
        a.btn-link(href=`${project.githublink}` target="_blank") GitHub Repo

    .project-img-box.box

      //-  add your project images, and add descriptions to the alt attributes
      img.project-img(src=`${project.projectimg1}`, alt=`${project.img1alt}`)
      img.project-img(src=`${project.projectimg2}`, alt=`${project.img2alt}`)
      img.project-img(src=`${project.projectimg3}`, alt=`${project.img3alt}`)

    nav.nav
      a(href='/')
        span &larr;
        |  Back
